document.addEventListener('DOMContentLoaded',()=>{const signUp=document.getElementById('signUpBtn');if(!signUp)return;signUp.addEventListener('click',async()=>{const msg=document.getElementById('authMsg');const email=document.getElementById('email')?.value.trim();const password=document.getElementById('password')?.value||'';if(!email){msg.textContent='Enter your email first.';return}if(password.length<6){msg.textContent='Use a password with at least 6 characters.';return}if(!sb){msg.textContent='Login is still loading…';return}msg.textContent='Creating your Moneyology account…';msg.classList.remove('confirm-bubble');const {data,error}=await sb.auth.signUp({email,password,options:{emailRedirectTo:location.origin+location.pathname}});if(error){msg.textContent=error.message;return}if(data?.session){msg.textContent='Account created — you’re signed in! 💖';return}msg.classList.add('confirm-bubble');msg.innerHTML=`<strong>Hey! 💌 Go confirm your email!</strong><span>We sent a confirmation link to <b>${email.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}</b>.</span><span>Tap the link in that email, then come back here and press <b>Sign in</b>. ✨</span>`;});});